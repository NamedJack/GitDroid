<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.zx.gitdroid.activity.LeadActivity">

    <!--<android.support.v4.view.ViewPager-->
        <!--android:id="@+id/lead_pager"-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="match_parent">-->
    <!--</android.support.v4.view.ViewPager>-->

    <LinearLayout
        android:layout_alignParentBottom="true"
        android:background="@color/colorBlue"
        android:id="@+id/btn_bar"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="40dp">
        <TextView
            android:id="@+id/btn_login"
            android:clickable="true"
            android:gravity="center"
            android:textColor="@color/white"
            android:text="登录GITHUB"
            android:textSize="14sp"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:layout_height="match_parent" />
        <TextView
            android:id="@+id/btn_use"
            android:clickable="true"
            android:gravity="center"
            android:textColor="@color/white"
            android:text="直接使用"
            android:textSize="14sp"
            android:layout_weight="1"
            android:layout_width="0dp"
            android:layout_height="match_parent" />
    </LinearLayout>
    <fragment
        android:name="com.zx.gitdroid.fragment.LeadFragment"
        android:id="@+id/lead_fragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/btn_bar"/>
</RelativeLayout>
